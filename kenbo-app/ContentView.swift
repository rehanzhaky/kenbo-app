import SwiftUI
import SwiftData

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Hi, Kenbo User")
                .font(.largeTitle)
                .fontWeight(.bold)
                .padding()
        }
    }
}

#Preview {
    ContentView()
}
